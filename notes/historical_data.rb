class HistoricalData
  def self.find_stocks(args)
    # will also clean up stocks (market cap, last pricing date, stock price of 0)
  end

  def self.all_periods
    # will return array of strings representing periods
  end
end
